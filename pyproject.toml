[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "excusegen"
version = "0.1.2"
description = "Generates bad excuses"
authors = [
    { name = "Team Ember" }
]
readme = "README.md"
requires-python = ">=3.8"
